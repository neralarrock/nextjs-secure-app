# Copy to `.env.local` for local development. Do not commit `.env.local`.

# Safe to expose to the browser (embedded in client bundles).
NEXT_PUBLIC_APP_NAME=My Secure App

# Server-only — use in API routes, getServerSideProps, etc. Never expose in client code.
SERVER_ONLY_TOKEN=replace-with-a-long-random-string


##HI there